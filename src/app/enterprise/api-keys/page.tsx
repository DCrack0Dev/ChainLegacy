import EnterpriseSkeleton from '@/components/enterprise/EnterpriseSkeleton';
export default function P() { return <EnterpriseSkeleton eyebrow="API Keys" title="API Keys" subtitle="SHA-256 hashed secrets only. Prefix clsbox_ = sandbox (safe for dev/test). Prefix clprod_ = production (never show again after create). Rotation auto-revokes old key." stats={[{label:'Sandbox keys',value:1},{label:'Production keys',value:0},{label:'Revoked',value:0},{label:'Default scopes',value:17}]} cta={[{label:'+ Create Sandbox Key',variant:'primary'},{label:'+ Create Production Key',variant:'secondary'}]}><div className="rounded-2xl border border-white/10 p-6 text-sm text-gray-400">SIMULATION · POST /api/v1/api-keys returns secret once. DELETE /api/v1/api-keys revokes.</div></EnterpriseSkeleton>;}
